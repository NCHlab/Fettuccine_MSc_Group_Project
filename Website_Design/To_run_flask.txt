ON WINDOWS:

OPEN COMMAND PROMPT, GO TO CORRECT DIRECTORY (cd ..\..)
set FLASK_APP=myapp.py (replace with name of file)
set FLASK_DEBUG=1

flask run


ON LINUX:
OPEN TERMINAL, GO TO CORRECT DIRECTORY (cd..\..)
export FLASK_APP=myapp.py (replace with name of file)
export FLASK_DEBUG=1

flask run
